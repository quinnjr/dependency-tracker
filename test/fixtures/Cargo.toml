[package]
name = "example"
version = "0.1.0"

[dependencies]
serde = "1.0.210"
serde_json = { version = "1.0.128" }
tokio = { version = "1.40", features = ["full"] }
local_thing = { path = "../local_thing" }
exact_dep = "=1.2.3"

[dev-dependencies]
criterion = "0.5.1"
